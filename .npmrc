# No ejecutar scripts de postinstall de dependencias (seguridad)
ignore-scripts=true

# Permitir que pnpm rehaga node_modules sin prompt interactivo
# (necesario para `make start`, CI y cualquier shell sin TTY)
confirm-modules-purge=false
